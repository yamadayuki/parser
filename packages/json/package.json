{
  "name": "@yamadayuki/json",
  "version": "0.0.0",
  "main": "lib/index",
  "types": "lib/index",
  "scripts": {
    "prebuild": "npm run clean",
    "build": "tsc -p .",
    "build:watch": "tsc -p . -w",
    "clean": "rm -rf lib",
    "test": "jest -c ./jest.config.js",
    "test:update": "jest -c ./jest.config.js -u",
    "coverage": "jest -c ./jest.config.js --collectCoverage"
  },
  "keywords": [],
  "author": "Yuki Yamada <yamadayuuki0818@gmail.com>",
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.0.10",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.2"
  }
}
